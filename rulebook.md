# 変更ルール

1. ユーザーの最新指示と `current-spec.md` を優先する。
2. ローカル実装、配布ビルド、公開GAS、Google Sheetsを別々に管理する。
3. 問題の正本は提示Sheets。通信失敗時に古いデータへ無言で切り替えない。
4. 既存の採点、ユーザーキー、保存形式、公開範囲を無関係な変更で変えない。
5. `regression-checklist.md` に従い、ビルドと実画面を別々に確認する。
6. 機能試験では本番POSTを遮断。実データのローカル試験はGAS疎通成功とは報告しない。
7. mainへのpushは公開を伴う。許可なしにpush・デプロイしない。
8. 修正理由と未確認事項を `change-log.md` に残す。

## 色メタデータの運用

色の正本はinorganic!I:M。元のA:Hを自動補正せず、本文に明示された色のみ装飾する。内容が曖昧ならメタデータを空欄にしてvisual_reviewへ理由を残す。公開フロントの反映とSheets反映は別に確認する。

添字は表示専用。化学式の数字だけを装飾し、採点用原文を変えない。電荷を曖昧に記す多原子式は^で明示する。

## 教材候補の編集原則（2026-09-12）

正解が一意になる条件を先に記載し、誤答は同じ学習軸の混同から作る。誤答数のために曖昧な候補を足さない。選択肢が欠けた問題を他行から無作為に補わない。分類の上位下位関係を正誤判定で混在させない。構造式の見た目で区別できない異性体を同じ設問の候補にしない。変更はlearning JSONと既存Sheetsの対応列へ同期し、読み戻し後に公開フロントを検証する。旧仕様の件数と候補生成はcurrent-specの最新節を優先する。


単元分類はquestion_idで管理し、Sheets行番号で判断しない。教材追加時は単元マップを更新し、90既存IDの重複・漏れと各出題タイプの適格件数を検証する。単元／全体のシャッフルを混同せず、選択肢シャッフルは独立して維持する。新旧の成績範囲キーを混ぜない。


読込改善では遅延成功・永久待機・手動再試行を別々に検証する。問題と成績の失敗を混同しない。問題のキャッシュは検証済み成功データだけを短時間再利用し、旧教材や失敗を成功として返さない。タイムアウト時の自動再接続は行わない。


化学式の添字分割をflex直下へ並べない。UI確認では枠外文字だけでなく、隣接文字の間隔とNextの実寸・クリック遷移も検証する。


2026-09-13: 問題範囲は初期10問一覧を表示し、区切りと全体出題の切替を同一画面で完結する。選択済みボタンで一覧を閉じず、ALLの履歴キーは従来と同じ全問扱いを維持する。


無機設定は単元一覧を優先。3モードは最上部でコンパクトな横列とし、設定領域の横幅を活用する。学習動作は表示修正から分離して維持する。


問題数選択では選択済みの出題タイプを維持表示し、名称はタイプ選択と同一の定義を使用する。HTTP失敗の発生箇所と推測原因を分ける。教材件数と独立した学習内容の網羅性を混同しない。

- 2026-09-13：知識4択のcategory列は空ならorganic、inorganicは無機D。既存実験IDと並び順を保持し、成績のcategoryとmodeの両方を揃える。問題追加時は新カテゴリ対応フロントを公開してからSheetsへ追加する。

## AI支援プレビューの境界（2026-09-13）

模擬応答をAI回答として表示しない。DEV・loopback・明示クエリの条件をすべて満たす場合だけ表示する。キーをVITE変数に入れない。実APIを導入する際は別工程で鍵の利用判断・サーバーの認証と上限・教材版を確定する。クライアントの回数制限だけで公開しない。AI状態を既存採点・保存へ流さず、AI欄の入力操作を次問イベントへ伝播させない。今回の模擬試験を実API成功や生成内容の正確性として扱わない。

## 実API接続の運用境界（2026-09-13）

公開GAS・Sheets・採点をAI接続のために変更しない。キーは承認されたMac内専用ファイルだけで保持。ローカルAPIのOrigin・セッション・教材署名・上限・日次台帳・重複防止を無効化しない。教材不一致時はAI欄だけ停止し、古い教材で無言の応答をしない。提供元失敗時は自動再試行しない。このMac用セッションを公開サービスの本人認証として扱わない。公開前に利用対象者とサーバー認証・日次予算を別途確認する。


## GAS AI境界（2026-09-13）

- 利用者は誰でも（ユーザー指定）。日次上限は利用者合計30回で、匿名セッションの再発行では解除しない。
- 既存GAS_CODE_UNIFIED.jsは成績用。gas-ai/Code.gsを既存成績用GASへ混在させない。
- APIキーはAI専用GASのScript Propertiesに置き、ソース・配布物へ入れない。
- gas-ai/Code.gsは再生成物。tutor.template.jsを正本とし、npm run ai:gas:build後に一括版一致を検査する。
- 未設定URL・ダミーURLの状態でAIの公開完了を報告しない。実GASと模擬試験、画面タイムアウトとGAS停止、自己検証と独立検証を区別する。


- 2026-09-13追記：GAS一括版の教材は日本語を保持した整形JSONで公開する。圧縮・Base64へ戻さない。教材の編集元と再生成・署名照合の規則を維持する。


## 2026-09-13 シート正本への設計訂正（最優先）

直前の「整形JSONで全教材をGASに記載する」方針は廃止。教材の正本は既存スプレッドシート。通常の教材編集にGASの再生成を要求しない。取得失敗時に古い教材を返さず、問題別署名照合・利用上限を維持する。Google権限の認可とコードが実際に行う読取操作を区別する。


## 2026-09-13 外部接続権限の復旧・公開準備

ユーザーがAI専用GASの外部接続権限（script.external_request）を承認。診断後、新しい /exec URLで実セッション開始とOpenAI解説応答（reply.status=ok）を確認。以前の失敗はUrlFetchApp.fetchの権限不足で、API送信前に停止していた。新URLをsrc/config/aiTutor.tsへ設定し、公開用ビルドを更新。既存の問題配信・成績保存GASとSheetsの内容は変更なし。独立セキュリティ検証・実iPad/Safari・全問の説明品質は未確認。GitHub Pagesへの反映と公開画面確認は後続工程。


### ブラウザ再確認成功・公開反映対象

ユーザー許可後の1回の再確認で、公開相当ビルドの画面から実GASのセッション開始とAI解説表示に成功（2要求、約20秒、画面例外なし）。Googleリダイレクト後の応答と有機実験org-e-001の結論・区別点・確認の問いを確認。問題配信と成績保存の通信は模擬化し、成績シートへ書き込まない。前回のタイムアウトも記録として維持し、通信が常に安定するという保証はしない。対象はAIパネル・AI専用接続・GAS/ローカルサーバー実装と関連文書。従来の出題・採点・問題/成績GASは変更なし。公開後の確認は別途記録する。


## 2026-09-13 AI公開反映・公開画面確認済み

AI接続版1f14e71をmainへpushし、GitHub Actions 34746018236のbuild/deploy成功を確認。公開先 https://myo-n778.github.io/chemistryapp/ で「AIに聞く」から実GAS→OpenAI→結論・区別点・確認の問いの表示を確認（org-e-001、2要求、約24秒、画面例外なし）。APIキー不混入を公開対象35ファイルと配布JSで検査。既存問題配信・成績保存のコードやシートは変更していない。公開画面試験では問題配信・保存を模擬化したため、本番の問題取得・成績保存を今回再検証したものではない。実iPad/Safari、独立セキュリティ検証、全問のAI説明品質、利用者受入は未確認。

GASの外部接続権限不足はユーザーの承認で解消。ブラウザの一時的なタイムアウトは自動再試行せず、明示許可後のローカル・公開画面試験で成功した。待機時間超過時は通常の演習を継続できる。利用上限は1問3回・1学習20回・全利用者合計1日30回。次は利用者による表示・操作確認。証拠 /private/tmp/chemistry-public-gas-ui-result.json、chemistry-public-gas-ui.png。Company WI-20260913-140909-3f3175。


## 2026-09-13 AI欄の大きさと配置を調整

ユーザーから有機・無機ともAI表示を確認した後、欄が大きいとの改善依頼。AI欄を最大760pxの中央配置とし、文字を16〜22pxの範囲で既存文字サイズに追従、余白を縮小。短い操作ボタン2つを横並びにし、自由入力は「自分の言葉で質問する」で開く。入力欄を2行に縮小。問題文・採点・教材・GAS・キー・利用上限は変更なし。

公開用ローカルビルドで320/844/1280px×有機①⑥・無機A/Dの12条件を確認。初期AI欄は狭幅338px、844/1280pxで262px。入力の折畳み・送信・履歴保持・Enterで問題が進まない・Next・横はみ出しなし・画面例外なしを確認。AI/問題/保存通信は模擬化し、今回の実AI追加呼出しや成績書込みはなし。TypeScript/Viteとdiff検査PASS。実iPad/Safariと新配置の利用者受入は未確認。公開反映後の確認はCompany結果へ記録。証拠 /private/tmp/chemistry-ai-compact-ui.log、chemistry-ai-compact-844.png。


## 2026-09-13 AIの分野別・通算表示

問題ごと・学習ごとの上限数値表示を廃止し、「有機 N回 · 通算 M回」または「無機 N回 · 通算 M回」に変更。現在の分野だけを表示。「問題と質問をOpenAIへ〜」の段落を削除。内部の1問3回・学習20回・全利用者1日30回の制限は維持。

回数は実接続モードでAI回答を表示できた場合だけ加算し、再開閉・通信失敗・次へ進んだための中断・操作プレビューは加算しない。今回からの計数で過去分の推計は行わない。保存先はこのブラウザのlocalStorageで利用者キー別（chem.aiUsage.v1）。有機と無機の和を通算とする。端末間同期なし。保存不可時はページ内だけで継続し、回答表示を妨げない。GAS・教材・採点・成績保存は変更なし。

確認：TypeScript/Vite、差分検査、ローカル画面で分野切替・ページ再読込・別利用者への切替・元利用者への復帰・失敗/中断/再開閉で不加算・説明文撤去。保存異常・不正値・壊れたJSONも検査。AI・問題・保存通信は模擬、実AI追加呼出し・Sheets書込みなし。実端末・利用者受入は未確認。公開後の確認はCompany結果へ記録。証拠 /private/tmp/chemistry-ai-counter-ui.log。
