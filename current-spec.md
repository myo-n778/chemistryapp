# Chemistry Drill 現行仕様

更新: 2026-09-11。取得元 `main` / `20efce4`。修正版は `4e739e4` としてGitHub Pagesへ公開済み。

## 目的と範囲

有機6モード・無機3モードの化学演習。ユーザー、学習記録、採点・効果音の既存方式を維持する。
今回の範囲は起動・問題取得・無機CSV対応・学習入口の改善。その後ユーザー承認によりフロントを公開。GAS編集・Sheets更新は含まない。

## 接続と正本

- フロント: `src/`。React 18 / TypeScript / Vite。
- 問題ソース: Google Sheets `chemistry`、ID `1QxRAbYbN0tA3nmBgT7yL4HhnIPqW_QeFFkzGKkDLda0`。
- タブ: compounds / reactions / experiment / inorganic / rec / userStats。
- URL正本: `src/config/gasUrls.ts`。提示URLと既定値が一致。
- PROBLEM / REC / STATSは既定で同じ統合GAS。環境変数で上書き可能。
- 記録POSTはPROBLEMへ送信する既存仕様。RECは記録GET、STATSは成績GET。
- `GAS_CODE_UNIFIED.js` はローカルの統合候補。公開ソースとの同一性は未確認。
- `.env.txt`、`名称未設定.txt`、古い説明書のURLは現行設定ではない。
- `.github/workflows/deploy.yml` はmainへのpushで公開する設定。承認後、修正版をmainへpush済み。

## 復旧後の挙動

- 全Hooks実行後にユーザー未選択の画面を分岐し、作成・選択後もHooks順序を変えない。
- GASモードでは問題取得の失敗を空配列・古いCSV・旧localStorageの成功で隠さない。
- 同一の並行GETは共有し、45秒でタイムアウト（2026-09-13改訂）。自動再試行はせず、画面で「再読み込み」を選ぶ。
- 分野を離れた後の応答は画面状態へ反映しない。読み込み中も分野選択へ戻れる。
- 無機シートは見出し名で対応。現行の equation_tex / reactants_tex / products_tex / conditions / observations / reaction_ja / reaction_before_ja / reaction_after_ja と旧列名を受け付ける。
- CSVの引用符内改行・カンマ・二重引用符を保持する。未知の必須列はエラーにする。
- 無機の固定90件を廃止し、回答欄のある出題可能な行を数える（確認時A90 / B41 / C77）。
- 出題範囲はAppだけで切り出す。B/Cの二重sliceを廃止。選択肢は出題範囲とは別に全シート行を母集団とする。
- 構造式モードは構造式が有効な化合物を数える。
- KaTeX mhchemはインストール済みKaTeXから同梱。実行時CDNを不要とする。
- 入口は日本語の分野選択を先に配置。記録・ランキングは折りたたみ。診断パネルは `?diagnostics` 指定時のみ。

## 外部とデータの未解決事項

- 旧URLは匿名GETで403。2026-09-11 20:04以降、ユーザーの新デプロイURLへローカルを切替。問題4種・記録・成績GETは200。有機・無機の実問題をローカルChromeで表示確認済み。
- 公開フロントは `4e739e4`。Actions成功、配布JSのローカル一致、有機問題表示と、無機タイプB・11–20の4択表示・回答・Next表示を確認。実記録POSTとSheets読戻しは未確認。
- 読取したシートは化合物74、反応34、実験18、無機90のデータ行。機能試験での件数と教材内容の正確さは別。
- inorganic!E2:E3に酸との反応に対応しない「酸素発生（助燃性）」が含まれる。内容監査待ち、シート未変更。
- 本番記録保存、公開ランキング、実iPad/Safari、教材全問の正確性、利用者受入は未確認。
- 古い個別GASファイルは参考用として残す。最新仕様と矛盾する場合は本書を優先。

## 2026-09-11 文字サイズ切替（ローカル、未公開）

- 全画面上部に「小・中・大」を常設。初期値は中。
- root文字倍率は小100%・中125%・大150%。中は旧5段階の小に相当。既存のvw/clampによる調整は維持するため各部の実倍率は異なる。
- 保存先は端末内localStorageの `chem.textSize.v2`。旧 `chem.textSize` は xs→小、sm→中、md/lg/xl→大へ移行する。不正値・保存不可なら中で開始。
- サイズ変更だけでは問題・回答・得点・選択ユーザーをリセットしない。
- 全サイズで固定高さを解除。文字は枠内で折り返し、狭い画面の選択肢は1列、長い化学式は式内で横スクロール可能。
- ユーザー向け「TeX表示」はすべて「化学式表示」に統一。内部のTeX処理は維持。
- sound操作は文字サイズバーと重ならないよう画面内右寄せ。結果のオーバーレイはサイズバーの実測高さを避けて配置。
- GAS・Sheets・問題・採点・保存処理は変更しない。公開フロントは従来の4e739e4のまま。

- 無機A/B/Cの問題の通常表示と化学式表示は共通の中央配置枠を使用。長い通常文は折り返し、化学式は枠内スクロール。選択肢の整列は維持。

## 2026-09-12 異常画面のボタン修正（ローカル、未公開）

読込中・通信エラー・空データ画面はstatus-panel/status-actionへ分離。固定54×24pxの演習ボタンを継承せず、自動幅高さ・中央配置・狭幅縦並びにした。修正前は枠内22pxに文字43/53pxの縦はみ出しを再現。320×900・844×390・1280×900の小中大で読込中・15秒タイムアウトの文字Range四辺と枠を照合し、全9組通過。模擬再読込→モード選択、画像目視、TypeScript/Vite・diffも通過。実通信・実iPad・公開は未確認。GAS/Sheets・通信処理・採点は未変更。前段の異常画面・縦方向の確認漏れはAIFT-20260912-001へ記録。

## 2026-09-12 無機化学の色見本

Sheetsのinorganic!I:Mへreactants_visual / products_visual / observations_visual / visual_timing / visual_reviewを追加。既存A:H・90行の値は保持し、追加値と書式・入力規則を読み戻した。43問・66セルに元の本文で明示された色を登録。観察事項の整合性を確認できない25行は該当メタデータを空欄とし、M列へ確認理由を記録。元の教材本文の誤りは修正していない。
アプリは本文に既に書かれた色名の直前へ色付きの四角を表示する。正解行の色を別の選択肢へ流用せず、各選択肢の出典に照合する。初期データは「本文に合わせる」。行ごとに「解説のみ」「非表示」へ変更できる。旧シートと空欄は色なしで動作する。色は見本で、実物の濃度・照明による違いは再現しない。
Sheets変更は反映済み。既存GASの匿名GETでも追加列を含む13列・90行を確認し、GASの再デプロイは不要だった。フロント変更はローカルのみ・未公開。採点・問題本文・保存処理は維持。詳細はcolor-display-guide.md。

## 2026-09-12 公開反映完了

ユーザーの公開依頼により、色見本・三段階文字サイズ・中央配置・異常画面ボタンを5cd207bとしてmainへpush。GitHub Actions 34652838751成功。公開URL https://myo-n778.github.io/chemistryapp/ のJS/CSSがローカルdistと完全一致。公開サイトのChrome 390px幅・文字大・実GASデータで無機Aの11–20を開始し、4択・回答・色見本5個・解説・横はみ出しなしを確認し画像目視も通過。ブラウザ実行エラーなし。本番POSTは遮断し保存しなかった。Sheets追加列は先行反映済み、今回GAS/Sheetsは未変更。実iPad/Safari、全問内容、実記録保存、利用者受入は未確認。以前の未公開表記は各作業時点の履歴。

## 2026-09-12 通常表示の化学式添字

通常表示の無機問題・選択肢・解説の化学式をHTML sub/supで描画。Cl2→Cl₂、2H2→2H₂、Ca(OH)2→Ca(OH)₂。係数・水和物の先頭数は通常サイズ。Ca2+やFe3+の単原子イオン、NH4+、SO4^2-の明示的電荷を上付き表示。多原子イオンの電荷数はSO4^2-のように^で明示する。元素記号として成立する式だけを処理し、日付・温度・pH・英単語・既存Unicode添字は保持。色見本と共存し、TeX表示は既存KaTeXへ渡す。シートの原文・採点値・保存処理は未変更。
検証：TS/Viteとdiff通過。組成数・係数・括弧・水和物・電荷・通常数値の単体検証、320/844/1280pxの無機ABCの回答・色表示・旧データ互換・表示時期・枠検査通過。320px画像で添字の高さと通常係数を目視。初回は閉じ括弧後の数を候補抽出できず、抽出規則を修正して再検証した。実iPad/Safariと利用者受入は未確認。

公開確認：1f40e69を同じGitHub Pagesへ反映、Actions 34664033258成功。公開JS/CSSとdistの一致、公開アプリに検証用化学式を読ませた320px幅で下付き位置・通常係数・色表示・回答と解説・枠内表示を確認。検証用データはブラウザ内だけで使用しSheetsは未変更。

## 2026-09-12 暗記・試験対策向けの教材洗練

- 目的は難化ではなく、正しい知識を覚え、混同する点を区別できること。
- 全9モードで選択肢位置を出題時にFisher–Yatesでランダム化する。Sequential/Shuffleは問題順の指定で、選択肢の位置はどちらでもランダム。表示中・文字サイズ変更・回答後には並べ替えない。
- 生成物・条件・観察・有機反応は問題ごとの誤答候補を使用。構造問題は明示した近隣化合物IDを全化合物から参照し、選択した出題範囲の外も候補にできる。無関係な答えで4択を埋めず2〜4択を許す。
- 正誤どちらでも「覚えるポイント」を表示。化学式添字と色見本を維持し、誤答の色名にも同じ規則で色を付けて正解だけを目立たせない。
- 無機Cは生成物を含む反応式でなく、反応物と必要条件から観察を問う。Bは触媒・電極・液性など問う軸を明示する。
- 分類モードの誤った集約（単糖・アミノ酸→高分子、全エステル→油脂、ハロゲン化アルカン→アルカン）を撤廃。元の分類を保持し、互いに正解にならない分類候補を使う。p-ベンゾキノンの分類はキノンへ訂正。
- 教材編集用ミラーは `learning/*.json`。実行時の問題正本は引き続き同じSheets/GAS。スキーマと運用は `learning/README.md`。欠損・壊れた候補は読込エラーとして知らせ、旧ランダム候補に戻さない。
- 無機90行中2行、有機反応34行中2行は削除せず保留。実際の出題件数は無機A88 / B40 / C66、化合物74、有機反応32、分類実験18。無機の観察が不適切だった行を訂正・対象外化し、酸化・加水分解・電気分解の条件を明示した。
- Sheets更新済み。inorganic A:W / reactions A:J / experiment A:H、およびcompounds G:HとC74を対象とし、変更1541セル、照合対象2746セルは全値一致。化合物の構造データE:F・GASコード・rec/userStatsは変更していない。
- 検証：全件パーサ・誤答候補・ランダム位置・採点対応・分類誤判定の回帰、TypeScript/Vite、差分検査。Chrome 320×900 / 844×390 / 1280×900の9モード、文字大、回答・次問・ポイント・枠内表示を確認。実記録POSTはテストで遮断。Sheetsのネイティブ画面、実iPad/Safari、外部の独立教材審査、利用者受入は未確認。
- 公開フロントの反映確認は下記追記で管理する。

公開画面検証で、分類実験の範囲指定が1行ずれる既存不具合を発見。1始まりの開始番号を配列用に1減らし、1–10が1問目から、11–18が11問目から出るよう訂正した。

公開確認完了：教材・UIをae9ee23、分類実験の範囲修正を6b679ceとして同じGitHub Pagesへ反映。最終Actions 34671931493成功。公開JS/CSSがローカルdistと一致し、公開Chromeの実GASで無機Aの正答とポイント、酢酸メチルのエステル分類、分類実験の4番目の正答採点と文字サイズ変更後の配置保持を確認した。分類実験の1–10/11–18はローカルChromeと実GASで先頭問題・末尾件数を検証。公開画像も目視。GASコード・実記録保存は変更/実行していない。実iPad/Safari・独立教材審査・利用者受入は未確認。


## 無機の単元別学習（2026-09-12）

無機A/B/Cの設定を「単元順」「単元内ランダム」「完全ランダム」に変更。初期値は単元順。10単元は気体、沈殿、両性元素・錯イオン、金属、ハロゲン、分解、硫黄、窒素・リン、電気分解、酸化還元・液性。単元順は選択単元の定義順で全問、単元内ランダムはその単元の全問を混ぜ、完全ランダムは全単元から10/20/全問を重複なく抽出する。選択肢位置は従来どおりランダム。

単元順と単元内ランダムの結果から「次の単元へ」で進む。0問の単元は無効表示し、次への移動では飛ばす。単元切替で問題・スコアを初期化。開始・単元移動・設定に戻る際は画面先頭へ戻る。単元マップ正本はsrc/utils/inorganicUnits.tsで、Sheetsのquestion_idに対応し行順には依存しない。未登録IDは未分類として表示し消さない。保留は既存読込規則で除外し、A88/B40/C66は維持。

成績のrangeKeyをunits-v1＋出題モード＋単元＋問題数で分離する。保存先・保存形式・採点式・教材本文・Sheets/GAS・有機6モードは変更しない。旧範囲キーの履歴は保持する。


公開確認：9b7119aを同じGitHub Pagesへ反映。Actions 34673977769成功。公開JS/CSSがローカルdistと一致し、公開Chrome・実GASでA/B/C×3モードの開始・回答・覚えるポイントを確認。単元順Aの先頭は炭酸カルシウム＋塩酸。390px・文字大の公開画像を目視。本番POSTは遮断。Sheets/GASは今回変更せず、実iPad/Safari・実保存・独立検証・利用者受入は未確認。


## 2026-09-13 有機教材・成績の読込改善

症状：有機が全体の読込エラーになり、無機の成績・ランキングが読み込み中で止まる。公開Chromeで有機のGAS応答が15秒を超え、旧15秒タイムアウトで中断することを再現した。GAS側の遅延理由までは特定していない。初回失敗後の実GAS自動再試行は行っていない。

修正：有機は最初に出題タイプを選ぶ。①②③は化合物、④⑤は化合物と反応（構造表示に必要）、⑥は実験だけを取得する。不要な教材の遅延で全体を止めない。問題と成績のGETを共通化し、同じURLの並行取得を共有。45秒で本文取得まで打ち切り、失敗は自動再試行しない。検証済みの問題データのみメモリ内で60秒再利用し、失敗応答と空データはキャッシュしない。教材ファイルへの自動フォールバックはない。

成績・ランキングの読み込み表示を区別し、成績を待たず出題できる旨を表示する。失敗を空記録として隠さず、説明と手動再読込ボタンを表示。成績と直近平均のGETは同時に開始して総待機時間を45秒以内にする。画面を離れた後の応答を破棄する。ランキングのエラー/待機欄のみ既存高さ上限を外し、大文字のボタン重なりを修正。成功したランキング表の高さ制限は維持する。

維持：教材・正誤判定・単元モード・保存POST・Sheets・GASコード・公開URL。今回はフロントのGETと表示だけを変更。

検証：TypeScript/Vite、git diff --check、scripts/validate-loading.cjsの模擬通信試験（16秒応答成功、45秒タイムアウト、同時要求共有、再試行なし、手動再試行、エラー非キャッシュ、成功データ再利用、他教材障害の分離）。Chrome320/844/1280pxのエラーボタン枠とクリック、有機6モードの320pxで3問回答・次問・ポイント、無機ABC×3学習モード×3幅・単元完了と次単元を確認。本番POSTは遮断。

公開後の実GAS再接続はユーザー確認待ち。実iPad/Safari・実保存・独立検証・利用者受入は未確認。証拠 /private/tmp/chemistry-loading-probe.log、chemistry-loading-validation.log、chemistry-loading-organic.log、chemistry-loading-inorganic.log、chemistry-loading-build.log。


公開：44c2292、Actions 34730839360成功。公開JS/CSSはローカルdistと一致。公開Chromeにブラウザ内の模擬GAS応答を渡し、16秒応答、45秒停止、重複GET共有、再試行なし、手動再読込、失敗非キャッシュ、不要教材の障害分離を確認。実GASへの再接続ではない。初回の実通信タイムアウト後、ユーザーに再試行可否を質問済みで回答待ち。Companyはreview。


## 2026-09-13 化学式の文字分散とNextの修正

ChemicalTextがFragmentで複数のspan/subを返し、選択肢のflex/space-betweenの直下で各文字が分散していた。外側を1つのinline spanにまとめ、添字・上付きと元の文字列を保持した。Nextは全有機モードで読み込むQuiz.cssへ共通スタイルを追加。最小88×56 CSS px、文字サイズに追従する1rem、折返し、フォーカス表示を設定した。採点・教材・Sheets/GAS・読込処理は変更しない。

検証：同じフェノール/エタノールの設問で、FeCl3・NaHCO3・金属Naの文字間隔と添字を照合。320/844/1280px、CSS zoom 0.8による80%相当表示でNextの高さ44px以上と連続4問の遷移を確認。実際のChromeブラウザズーム操作とは別の検証条件。画像目視、TypeScript/Viteとdiff検査、有機6モード・無機ABC×3モード×3幅の回帰を通過。実GAS再接続は行わず、ブラウザ内の検証データを使用。本番POSTは遮断。実iPad/Safari・利用者受入は未確認。選択肢順は変わり得るため、同じ設問を検証用の3/4問目へ配置した。

確認漏れはAIFT-20260913-001へ記録。枠内表示だけでなく、文字間隔と操作寸法を回帰項目にした。


公開確認：33bd2b0、Actions 34731324461成功。公開JS/CSSとdistの一致、公開Chromeの同じ設問の文字間隔・添字・Next実寸・連続4問クリックを4画面条件で確認。公開試験の問題はブラウザ内の模擬GASで供給し、Sheetsは変更せず、実GASへの再試行もしていない。ユーザーからは有機分類実験が出題されたことを画像で確認できた。全成績通信の復旧とは区別する。


## 2026-09-13 問題範囲の選択画面を統合

有機の問題数選択は初期状態で10問区切りの範囲一覧を表示する。同じ選択ボタンを押しても一覧を閉じない。All Questions/10ずつ/20ずつ/40ずつを同じ画面で切り替え、All Questionsの解答数も画面内で選ぶ。順序切替を引き続き操作でき、ALLは既存のnullを渡して全問と履歴キーを維持する。AllQuestionCountSelectorに埋込表示を追加し、既存の件数・履歴表示を再利用。無機単元設定、問題本文、採点、保存、Sheets/GASは変更しない。


検証：TypeScript/Vite、差分検査、Chrome320/844/1280px・文字大で初期一覧と同一ボタン再押下、全切替、順序変更、10問/全74問開始を確認。有機6モードで回答・次問・ポイントの回帰を通過。試験は模擬GASで、本番保存POSTを遮断。実iPad/Safariと利用者受入は未確認。


公開確認：3a05f60、Actions 34731716316成功。公開JS/CSSとローカルdistの一致、公開Chrome3幅で初期一覧・同一画面切替・順序変更・10問/全74問開始を確認。公開試験も模擬GASを使用し実通信・保存は遮断。証拠 /private/tmp/chemistry-range-public.log。


## 2026-09-13 無機の単元選択を優先する配置

単元順・単元内ランダム・完全ランダムを設定領域の最上部で常に横3列の小さな切替として表示。カード内の説明はtitleへ移動。重複した見出しと導入文を整理し、学習する単元を上へ寄せた。幅未指定で狭くなっていた設定領域をwidth:100%、最大1100pxにし、広い画面は単元カード3列以上を表示。狭い画面は読みやすい1列を維持。単元の選択、無効単元、問題数、順序、シャッフル、成績キー、採点、Sheets/GASは変更しない。


検証：TypeScript/Viteとdiff検査、Chrome320/844/1280pxおよびCSS zoom .8の4条件・文字大でABC×3学習モードの選択・出題・回答・戻るを確認。3切替は最上部の横一列、広い画面は単元4列。画像目視済み。実GASの再試行と保存POSTは行わず模擬応答を使用。実iPad/Safari・利用者受入は未確認。


公開確認：2d02a8c、Actions 34732101095成功。公開JS/CSSとdistが一致し、公開Chromeの4画面条件×ABC×3モードで配置・単元選択・開始・回答・戻るを確認。公開試験も模擬GASで実通信と本番保存を遮断。証拠 /private/tmp/chemistry-unit-compact-public.log。実iPad/Safari・利用者受入は未確認。


## 2026-09-13 選択タイプの表示と通信・教材範囲の調査

有機6タイプの問題数選択に「有機化学 · ① 構造式から名称」等の選択済みタイプを表示。quizModeLabels.tsを出題タイプ選択と共用し、同じ表示名を維持する。All/10/20/40の切替とクイズから戻ったときも表示し、All埋込では重複させない。問題・採点・読込・保存・無機設定・GAS/Sheetsは変更しない。

検証：TypeScript/Vite、差分検査、scripts/validate-selected-mode.cjsでChrome320/844/1280px・文字大の全6タイプ、区切り切替、出題・回答・戻る、ラベル一致・枠内表示を確認。通信は模擬応答で、本番POSTは遮断。

無機の一時HTTP404は問題解析前のHTTP失敗で、前回のタイムアウトと別事象。実通信の1回確認についてユーザー回答待ち。原因をキャッシュや混雑と断定せず、現状のGAS依存と未確認点をreports/2026-09-13-inorganic-404.mdへ記録。

受験範囲の点検をreports/2026-09-13-entrance-coverage.mdへ記録。ローカル有効行は無機88、有機化合物74、反応32、実験18。同じ教材の出題方向を変えるモードは独立知識として重複加算しない。現行教材だけで大学受験範囲を網羅するとは判断できず、異性体・構造決定・タンパク質・系統分離等の不足を整理。教材追加は未実施。
